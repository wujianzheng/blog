services:
  blog:
    build: .
    container_name: flask-blog
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
      - ./data:/app/data
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    restart: unless-stopped
